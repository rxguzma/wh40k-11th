name: Patch CSV

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-csv.yml'
  workflow_dispatch:
    inputs:
      patches:
        description: JSON list of operations. One run, one commit.
        required: true
        type: string

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Apply patches
        env:
          PATCHES: ${{ inputs.patches || '[{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"MASTER_OF_SHADOWS","mode":"update","sets":{"Ability Name":"Master of Shadows","Short_Description":"COMMAND\u003a friendly ADEPTUS ASTARTES INFANTRY\nRE-ROLL CHARGE until next COMMAND","Long_Description":"In your Command phase, you can select one friendly ADEPTUS ASTARTES INFANTRY unit. You can re-roll charge rolls for that unit until the start of your next Command phase.","Tags":"[RE-ROLL CHARGE]","Tag Categories":"[Movement Rules]","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"BLACKWING_MANTLE","mode":"update","sets":{"Ability Name":"Blackwing Mantle","Short_Description":"1/PHASE/ARMY\u003a HEROIC INTERVENTION on this unit\n-1 CP; does not consume phase use","Long_Description":"(Once per phase, per army) You can target this unit with the Heroic Intervention stratagem, regardless of any other uses of that stratagem this phase. If you do\u003a\n\u2022 That use is -1 CP.\n\u2022 That use does not prevent any uses of that stratagem on other units this phase.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"SHADOWMASTER","mode":"update","sets":{"Ability Name":"Trifold Path of Shadow","Short_Description":"STEALTH / 3\" DETECTION RANGE","Long_Description":"This unit has\u003a\n\u2022 Stealth.\n\u2022 3\" detection range.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"ECHO_OF_THE_RAVENSPIRE","mode":"update","sets":{"Ability Name":"Echo of the Ravenspire","Short_Description":"END ENEMY FIGHT, if UNENGAGED \u2192 STRATEGIC RESERVES","Long_Description":"At the end of your opponent\u2019s Fight phase, if this unit is unengaged, you can place this unit in strategic reserves.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"FORGEFATHER","mode":"update","sets":{"Ability Name":"Forgefather","Short_Description":"SHOOTING\u003a visible enemy within 24\"\nFriendly MELTA / TORRENT vs target\u003a +2 S","Long_Description":"In your Shooting phase, select one visible enemy unit within 24\" of this model. Friendly ADEPTUS ASTARTES units\u2019 [MELTA] / [TORRENT] attacks that target that enemy unit have +2 S.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"SEEKER_OF_THE_UNFOUND","mode":"update","sets":{"Ability Name":"Seeker of the Unfound","Short_Description":"First setup\u003a select 1 objective\nWithin range\u003a OC 10 / LD 5+ / FNP 4+","Long_Description":"The first time this model is set up on the battlefield, select one objective on the battlefield. While this model is within range of that objective, this model has\u003a\n\u2022 10 OC.\n\u2022 5+ Ld.\n\u2022 Feel No Pain 4+.","Tags":"[FNP 4+]","Tag Categories":"[Unit Rules]","Order":"0","Visibility_ID":"1","Default_Active":"0"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"LORD_OF_THE_PYROCLASTS","mode":"update","sets":{"Ability Name":"Lord of the Pyroclasts","Short_Description":"Enemy ENGAGED with this unit\u003a -1 OC","Long_Description":"While an enemy unit is engaged with this unit, that enemy unit has -1 OC.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"UNTO_THE_ANVIL","mode":"update","sets":{"Ability Name":"Unto the Anvil","Short_Description":"MELEE\u003a RE-ROLL WOUND 1S\nASSAULT DOCTRINE\u003a RE-ROLL ALL WOUNDS","Long_Description":"This unit\u2019s melee attacks can\u003a\n\u2022 Re-roll wound rolls of 1.\n\u2022 Or, if the assault doctrine is active for this unit, re-roll wound rolls.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"COLD_AND_CALCULATING","mode":"update","sets":{"Ability Name":"Cold and Calculating","Short_Description":"ATTACK\u003a choose LETHAL HITS vs MONSTER/VEHICLE OR SUSTAINED HITS 1 vs others\nTACTICAL\u003a both","Long_Description":"In your Shooting phase or the Fight phase, when this unit is selected to attack, you can select one of the following for this unit\u2019s attacks to have\u003a\n\u2022 [LETHAL HITS\u003a MONSTER/VEHICLE].\n\u2022 Or, [SUSTAINED HITS 1\u003a non-MONSTER/VEHICLE].\n\u2022 Or, if the tactical doctrine is active for this unit, [LETHAL HITS\u003a MONSTER/VEHICLE] and [SUSTAINED HITS 1\u003a non-MONSTER/VEHICLE].","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"CEREBREX_LOGIC_ENGINE","mode":"update","sets":{"Ability Name":"Cerebrex Logic Engine","Short_Description":"BATTLE FORMATIONS\u003a friendly ADEPTUS ASTARTES INFANTRY gains SCOUTS 6\"","Long_Description":"In the Declare Battle Formations step, you can select one friendly ADEPTUS ASTARTES INFANTRY unit. That unit has Scouts 6\".","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"MASTER_OF_THE_FORGE","mode":"update","sets":{"Ability Name":"Master of the Forge","Short_Description":"MOVEMENT start/end\u003a friendly VEHICLE within 3\"\nHeal 3; ignore BS / Hit / Wound modifiers until next MOVEMENT","Long_Description":"In your Movement phase, at the start or end of this unit\u2019s move, you can select one friendly ADEPTUS ASTARTES VEHICLE model within 3\" of this model\u003a\n\u2022 That VEHICLE model heals 3 wounds.\n\u2022 That VEHICLE model\u2019s attacks can ignore modifiers to the following until the start of your next Movement phase\u003a\n\u2022 BS.\n\u2022 Hit rolls and wound rolls.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"RITES_OF_TEMPERING","mode":"update","sets":{"Ability Name":"Rites of Tempering","Short_Description":"Attacks S > this unit T\u003a -1 TO WOUND","Long_Description":"Attacks that target this unit with a S greater than this unit\u2019s T have -1 to wound rolls.","Tags":"","Tag Categories":"","Order":"0","Visibility_ID":"1","Default_Active":"1"}},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"IRON_FATHER","mode":"update","sets":{"Ability Name":"Iron Father","Short_Description":"Within 3\" friendly ADEPTUS ASTARTES VEHICLE \u2192 LONE OPERATIVE","Long_Description":"While this model is within 3\" of a friendly ADEPTUS ASTARTES VEHICLE unit, this unit has Lone Operative.","Tags":"[LONE OPERATIVE]","Tag Categories":"[Unit Rules]","Order":"0","Visibility_ID":"1","Default_Active":"0"}},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"UNPARALLELED_TACTICIAN","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"CHAPTER_MASTER_OF_THE_RAVEN_GUARD","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"AETHON_SHAAN_AS_CHAPTER_MASTER","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER_VULKAN","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"CAANOK_HAMMER","mode":"delete"},{"file":"data/marines/Unit_Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER_FEIRROS","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"UNPARALLELED_TACTICIAN","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"CHAPTER_MASTER_OF_THE_RAVEN_GUARD","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"AETHON_SHAAN_AS_CHAPTER_MASTER","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER_VULKAN","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"CAANOK_HAMMER","mode":"delete"},{"file":"data/marines/Abilities.csv","id_column":"Ability_ID","row_id":"INSPIRING_COMMANDER_FEIRROS","mode":"delete"}]' }}
        run: |
          python3 <<'PY'
          import csv, json, os, sys
          from pathlib import Path

          raw = os.environ.get('PATCHES') or ''
          if not raw.strip():
              print('no PATCHES input; skip apply')
              raise SystemExit(0)
          try:
              ops = json.loads(raw)
          except json.JSONDecodeError as e:
              sys.exit(f'patches is not JSON: {e}')
          if not isinstance(ops, list) or not ops:
              sys.exit('patches must be a non-empty JSON list')

          def decode(val):
              if val is None:
                  return ''
              s = str(val)
              return s.replace('\\n', '\n')

          def load_csv(path):
              if path.is_absolute() or '..' in path.parts:
                  sys.exit(f'bad path {path}')
              if not str(path).startswith('data/') or path.suffix.lower() != '.csv':
                  sys.exit(f'file must be a .csv under data/: {path}')
              if not path.is_file():
                  sys.exit(f'missing {path}')
              with path.open(encoding='utf-8', newline='') as f:
                  rows = list(csv.reader(f))
              if not rows:
                  sys.exit(f'empty csv {path}')
              return rows

          files = {}
          changed = set()

          for i, op in enumerate(ops):
              if not isinstance(op, dict):
                  sys.exit(f'op {i} is not an object')
              path = Path(str(op.get('file', '')).strip())
              if path not in files:
                  files[path] = load_csv(path)
              rows = files[path]
              header = rows[0]
              id_col = str(op.get('id_column') or '').strip() or header[0]
              if id_col not in header:
                  sys.exit(f'op {i}: id column not in header: {id_col}')
              i_id = header.index(id_col)
              row_id = str(op.get('row_id') or '').strip()
              if not row_id:
                  sys.exit(f'op {i}: row_id required')
              mode = str(op.get('mode') or 'update').strip()

              if mode == 'add_column':
                  col = str(op.get('column') or '').strip()
                  if not col:
                      sys.exit(f'op {i}: add_column needs column')
                  if col in header:
                      print(f'column already exists {path} {col}')
                      continue
                  after_col = str(op.get('after_column') or '').strip()
                  if after_col and after_col not in header:
                      sys.exit(f'op {i}: after column not in header: {after_col}')
                  insert_at = header.index(after_col) + 1 if after_col else len(header)
                  old_width = len(header)
                  header.insert(insert_at, col)
                  for row in rows[1:]:
                      while len(row) < old_width:
                          row.append('')
                      row.insert(insert_at, '')
                  changed.add(path)
                  print(f'added column {path} {col}')
                  continue

              if mode == 'remove_column':
                  col = str(op.get('column') or '').strip()
                  if not col:
                      sys.exit(f'op {i}: remove_column needs column')
                  if col not in header:
                      sys.exit(f'op {i}: column not in header: {col}')
                  remove_at = header.index(col)
                  header.pop(remove_at)
                  for row in rows[1:]:
                      if len(row) > remove_at:
                          row.pop(remove_at)
                  changed.add(path)
                  print(f'removed column {path} {col}')
                  continue

              if mode == 'dedupe_by_key':
                  key_col = str(op.get('key_column') or '').strip()
                  if key_col not in header:
                      sys.exit(f'op {i}: key column not in header: {key_col}')
                  i_key = header.index(key_col)
                  kept = [header]
                  seen = set()
                  matched = 0
                  removed = 0
                  for row in rows[1:]:
                      if not row:
                          continue
                      while len(row) < len(header):
                          row.append('')
                      if row[i_id] != row_id:
                          kept.append(row)
                          continue
                      matched += 1
                      key = row[i_key].strip()
                      if not key:
                          sys.exit(f'op {i}: blank {key_col} while deduping {row_id!r}')
                      if key in seen:
                          removed += 1
                          continue
                      seen.add(key)
                      kept.append(row)
                  if matched < 1:
                      sys.exit(f'op {i}: found no rows for {row_id!r}')
                  if removed < 1:
                      sys.exit(f'op {i}: found no duplicate rows for {row_id!r}')
                  files[path] = kept
                  changed.add(path)
                  print(f'deduped {path} {row_id}: kept {len(seen)} unique {key_col} values, removed {removed} duplicates')
                  continue

              if mode == 'fill_blank_where':
                  match_col = str(op.get('match_column') or '').strip()
                  col = str(op.get('column') or '').strip()
                  if match_col not in header:
                      sys.exit(f'op {i}: match column not in header: {match_col}')
                  if col not in header:
                      sys.exit(f'op {i}: column not in header: {col}')
                  match_value = decode(op.get('match_value')).strip()
                  new = decode(op.get('value'))
                  if not match_value:
                      sys.exit(f'op {i}: match_value required')
                  if new == '':
                      sys.exit(f'op {i}: value required')
                  i_match = header.index(match_col)
                  i_col = header.index(col)
                  hits = 0
                  for row in rows[1:]:
                      if not row:
                          continue
                      while len(row) < len(header):
                          row.append('')
                      if row[i_match].strip() == match_value and not row[i_col].strip():
                          row[i_col] = new
                          hits += 1
                  if hits < 1:
                      sys.exit(f'op {i}: found no blank {col} values where {match_col}={match_value!r}')
                  changed.add(path)
                  print(f'filled {hits} blank {col} values in {path} where {match_col}={match_value!r}')
                  continue

              if mode == 'replace_text':
                  col = str(op.get('column') or '').strip()
                  if col not in header:
                      sys.exit(f'op {i}: column not in header: {col}')
                  old = decode(op.get('from'))
                  new = decode(op.get('to'))
                  if not old:
                      sys.exit(f'op {i}: replace_text needs non-empty from')
                  i_col = header.index(col)
                  hits = 0
                  for row in rows[1:]:
                      if not row:
                          continue
                      while len(row) < len(header):
                          row.append('')
                      if old in row[i_col]:
                          row[i_col] = row[i_col].replace(old, new)
                          hits += 1
                  if hits < 1:
                      sys.exit(f'op {i}: replace_text found no matches for {old!r}')
                  changed.add(path)
                  print(f'replaced {old!r} in {hits} rows of {path} column {col}')
                  continue

              if mode == 'delete':
                  kept = [header]
                  hits = 0
                  for row in rows[1:]:
                      if not row:
                          continue
                      if len(row) > i_id and row[i_id] == row_id:
                          hits += 1
                          continue
                      kept.append(row)
                  if hits != 1:
                      sys.exit(f'op {i}: expected 1 row for {row_id!r}, found {hits}')
                  files[path] = kept
                  changed.add(path)
                  print(f'deleted {path} {row_id}')
                  continue

              if mode == 'insert':
                  existing = 0
                  for row in rows[1:]:
                      if row and len(row) > i_id and row[i_id] == row_id:
                          existing += 1
                  if existing:
                      sys.exit(f'op {i}: row already exists: {row_id}')
                  sets = op.get('sets')
                  if not isinstance(sets, dict) or not sets:
                      sys.exit(f'op {i}: insert needs sets')
                  new_row = [''] * len(header)
                  new_row[i_id] = row_id
                  for col, val in sets.items():
                      if col not in header:
                          sys.exit(f'op {i}: column not in header: {col}')
                      new_row[header.index(col)] = decode(val)
                  after = str(op.get('after_row_id') or '').strip()
                  if after:
                      placed = False
                      kept = [header]
                      for row in rows[1:]:
                          kept.append(row)
                          if row and len(row) > i_id and row[i_id] == after:
                              kept.append(new_row)
                              placed = True
                      if not placed:
                          sys.exit(f'op {i}: after_row_id not found: {after}')
                      files[path] = kept
                  else:
                      rows.append(new_row)
                      files[path] = rows
                  changed.add(path)
                  print(f'inserted {path} {row_id}')
                  continue

              if mode != 'update':
                  sys.exit(f'op {i}: unknown mode {mode}')

              sets = op.get('sets')
              if not isinstance(sets, dict) or not sets:
                  sets = {}
                  if op.get('column'):
                      sets[str(op['column'])] = op.get('value', '')
                  if op.get('column_2'):
                      sets[str(op['column_2'])] = op.get('value_2', '')
              if not sets:
                  sys.exit(f'op {i}: update needs sets or column/value')

              hits = 0
              for row in rows[1:]:
                  if not row:
                      continue
                  while len(row) < len(header):
                      row.append('')
                  if row[i_id] != row_id:
                      continue
                  for col, val in sets.items():
                      if col not in header:
                          sys.exit(f'op {i}: column not in header: {col}')
                      row[header.index(col)] = decode(val)
                  hits += 1
              if hits != 1:
                  sys.exit(f'op {i}: expected 1 row for {row_id!r}, found {hits}')
              changed.add(path)
              print(f'updated {path} {row_id} {list(sets)}')

          for path, rows in files.items():
              if path not in changed:
                  continue
              with path.open('w', newline='', encoding='utf-8') as f:
                  csv.writer(f, lineterminator='\n').writerows(rows)
          print('changed', ' '.join(str(p) for p in sorted(changed)))
          PY

      - name: Refresh manifests
        run: python3 scripts/data_schema.py manifest

      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- data
          if git diff --cached --quiet; then
            echo "No change"
            exit 0
          fi
          git commit -m "Patch CSV line items"
          git push