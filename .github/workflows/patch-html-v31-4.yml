name: Patch HTML V31.4

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-html-v31-4.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Patch and validate V31.4
        run: python3 scripts/patch_html_v31_4.py

      - name: Publish version snapshot
        run: python3 scripts/publish_html.py

      - name: Commit V31.4 canonical HTML and release history
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add WH40k_11th.html versions
          git commit -m "Fix View plus panel in V31.4"
          git push origin HEAD:main
