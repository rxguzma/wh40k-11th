name: Patch HTML App

on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch_html_v31_125_ability_grid_layout.py'
      - '.github/workflows/patch-html.yml'

permissions:
  contents: write

concurrency:
  group: patch-html-main
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Build V31.125 Ability grid layout
        run: python3 scripts/patch_html_v31_125_ability_grid_layout.py

      - name: Validate and generate release history
        run: python3 scripts/publish_html.py --root .

      - name: Commit HTML and release history
        shell: bash
        run: |
          set -euo pipefail
          git add WH40k_11th.html versions
          if git diff --cached --quiet; then
            echo "No HTML changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Build V31.125 Ability grid layout"
          git push origin HEAD:main
